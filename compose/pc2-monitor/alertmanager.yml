global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'bot-stub'

receivers:
- name: 'bot-stub'
  webhook_configs:
  - url: 'http://tg-bot-stub:80/webhook' # Шлем алерты в нашу заглушку
